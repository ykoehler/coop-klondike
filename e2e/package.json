{
  "name": "coop-klondike-e2e",
  "version": "1.0.0",
  "description": "End-to-end tests for Coop Klondike Flutter web app",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install-browsers": "playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}